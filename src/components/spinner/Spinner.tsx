import StyledSpinnerContainer from './Spinner.styled';

const LoadingSpinner = () => {
  return (
    <StyledSpinnerContainer data-testid="spinner">
      <div />
    </StyledSpinnerContainer>
  );
};

export default LoadingSpinner;
